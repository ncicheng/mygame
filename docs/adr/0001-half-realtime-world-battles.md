# 半实时持久世界战斗架构

持久大地图上的军队实时行军渲染，部队相遇时冻结切入独立的战斗实例结算，结束后回写结果。

采用此方案是因为全实时碰撞模拟（所有军队在地图实时互打）对单开发者负载过高，而纯回合制又与"帝国时代式实时观感"的核心诉求冲突。半实时保留了大地图上的行军可视性，同时把战斗逻辑收敛到实例内结算。

Status: accepted

> 兼容性说明：本 ADR 概念上仍然成立（大地图实时行军渲染 + 独立战斗实例结算），但已不再依赖 Socket.IO 推送——见 0004 无服务器化后，实时观感改为前端本地模拟实现。