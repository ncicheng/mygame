# 军团攻城（城池征服）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 MyGame 添加攻城：玩家可攻击敌方城池，服务器权威结算，获胜则占领城池（扩张领地）+ 掠夺资源。

**Architecture:** 新增 `sieges` 表 + SECURITY DEFINER RPC `resolve_siege`（复用 combat 公式与挑战系统的结算模式：双方真实战损、写战报、攻方获胜转移城池 owner）。数据层加 initiateSiege/fetchSieges；UI 在地图选中敌方城池触发。

**Tech Stack:** React 18 + Vite + TS、Supabase Postgres + RLS + RPC、现有 data.ts 模式。

## Global Constraints

- 领域词汇用 `CONTEXT.md`：军团/城池/领地/挑战/战力
- 攻城结算必须服务器权威（SECURITY DEFINER），客户端不能转移城池/改他人部队
- 复用 `shared/src/combat.ts` 公式（战力/胜率/胜15%败70%），与 resolve_pvp 一致
- 攻方获胜：转移 `cities.owner_user_id` 给攻击方 + 掠夺资源；失败：攻方战损
- 校验：登录、武将存在、城池存在、非自攻、守城方免战期
- 中文注释；2 空格；遵循 data.ts/Schema 模式；复用前端测试基建

---

### Task 1: sieges schema + resolve_siege RPC

**Files:**
- Modify: `supabase/schema.sql`

**Interfaces:**
- Produces:
  - `sieges` 表（id/attacker_user_id/target_city_id/status/result/created_at/resolved_at）+ RLS（双方读、攻击方写）
  - `resolve_siege(p_attacker_general_id text, p_target_city_id text) RETURNS jsonb` SECURITY DEFINER：校验→结算→战损→获胜转移城池+掠夺→写战报→更新 sieges

- [ ] **Step 1: sieges 表 + RLS**

```sql
CREATE TABLE sieges (
  id text PRIMARY KEY,
  attacker_user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  target_city_id text NOT NULL REFERENCES cities(id) ON DELETE CASCADE,
  status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','resolved','cancelled')),
  result text CHECK (result IN ('attacker_win','attacker_lose')),
  created_at timestamptz NOT NULL DEFAULT now(),
  resolved_at timestamptz
);
ALTER TABLE sieges ENABLE ROW LEVEL SECURITY;
CREATE POLICY sieges_select ON sieges FOR SELECT USING (auth.uid() IS NOT NULL);
CREATE POLICY sieges_insert ON sieges FOR INSERT WITH CHECK (auth.uid() = attacker_user_id);
CREATE POLICY sieges_delete ON sieges FOR DELETE USING (auth.uid() = attacker_user_id);
```

- [ ] **Step 2: resolve_siege RPC**（复用 resolve_pvp 结算逻辑：读双方武将/部队，镜像 combat 公式，战损，获胜转移 cities.owner_user_id + 掠夺 resources.gold/food，写 pvp 战报 + 更新 sieges）中文注释 + 事务原子

- [ ] **Step 3: 静态校验 + Commit**

```bash
git add supabase/schema.sql
git commit -m "feat: 攻城 schema + resolve_siege RPC"
```

---

### Task 2: 攻城数据层

**Files:**
- Modify: `frontend/src/data.ts`
- Test: `frontend/test/data.test.ts`

**Interfaces:**
- Produces:
  - `Siege` 接口（id/attackerUserId/targetCityId/status/result/createdAt）
  - `initiateSiege(userId, attackerGeneralId, targetCityId, client?)`（插 sieges + 调 resolve_siege RPC，p_ 参数名）
  - `fetchSieges(userId, client?)`
  - RPC 失败回滚插入的 sieges 行（同 challenge）

- [ ] **Step 1: TDD 测试**（initiateSiege 插 + RPC；fetchSieges 读）→ 实现 → 通过

- [ ] **Step 2: Commit**

```bash
git add frontend/src/data.ts frontend/test/data.test.ts
git commit -m "feat: 攻城数据层"
```

---

### Task 3: 攻城 UI

**Files:**
- Modify: `frontend/src/WorldView.tsx`、`frontend/src/MapBoard.tsx`、`frontend/src/RightColumn.tsx`

**Interfaces:**
- Consumes: data.ts 的 initiateSiege/fetchSieges、敌方城池（side='enemy'）
- Produces: 选中敌方城池 → "攻城"按钮 → 调用 initiateSiege；攻城结果（胜/负 + 是否占领）展示

- [ ] **Step 1: 地图选中敌方城池 → 攻城按钮**

MapBoard/WorldView：选中敌方（side='enemy'）城池显示"攻城"。

- [ ] **Step 2: 攻城结果显示**（fetchSieges，胜/负 + 占领提示）

- [ ] **Step 3: 构建 + 测试 + Commit**

```bash
git add frontend/src/WorldView.tsx frontend/src/MapBoard.tsx frontend/src/RightColumn.tsx
git commit -m "feat: 攻城 UI"
```

---

## Self-Review

**Spec 覆盖：** sieges schema + RPC（Task1）、数据层（Task2）、UI（Task3）——覆盖。
**Placeholder 扫描：** 无 TBD。
**类型一致性：** Siege 接口 Task2 定义、Task3 消费；resolve_siege 参数名 p_ 前后一致。
