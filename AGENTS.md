## Agent skills

### Issue tracker

Issues 和 specs 以 markdown 文件形式放在 `.scratch/<feature>/` 下（本地 markdown，无远程仓库）。详见 `docs/agents/issue-tracker.md`。

### Triage labels

默认五个角色标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文：根目录一个 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。